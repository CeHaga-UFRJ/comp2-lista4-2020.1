package library.controller;

import library.entities.*;

import java.util.*;

class StatsCalculator {
    List<Borrow> lastNBorrows(int n){
        DataManager dm = DataManager.getDataManager();
    }
}
